name: Daily Signup Funnel Report

on:
  schedule:
    # 8:00 AM IST = 02:30 UTC
    - cron: "30 2 * * *"
  workflow_dispatch:  # manual trigger for testing

jobs:
  report:
    name: Post Amplitude → Slack report
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: funnel-optimization-agent/agent/requirements.txt

      - name: Install dependencies
        run: pip install -r funnel-optimization-agent/agent/requirements.txt

      - name: Run signup funnel report
        working-directory: funnel-optimization-agent/agent
        env:
          AMPLITUDE_API_KEY:    ${{ secrets.AMPLITUDE_API_KEY }}
          AMPLITUDE_SECRET_KEY: ${{ secrets.AMPLITUDE_SECRET_KEY }}
          SLACK_WEBHOOK_URL:    ${{ secrets.SLACK_WEBHOOK_URL }}
          AWS_ACCESS_KEY_ID:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION:    ap-south-1
        run: python3 agent.py
